<UserControl x:Class="Guardarropa.Views.CierreZView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource FondoPrincipal}">
        <Border Background="{DynamicResource FondoTarjeta}" CornerRadius="12" Padding="40"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                MinWidth="420" MaxWidth="500">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Opacity="0.3" ShadowDepth="5"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="CIERRE Z" FontSize="24" FontWeight="Bold"
                           Foreground="{DynamicResource ColorAccento}" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Antes del cierre -->
                <StackPanel Visibility="{Binding CierreRealizado, Converter={StaticResource InverseBoolToVisibility}}">
                    <TextBlock Text="Introduce la contrasena Z para realizar el cierre:"
                               Foreground="{DynamicResource TextoPrincipal}" FontSize="13" Margin="0,0,0,10"/>
                    <PasswordBox x:Name="TxtContrasenaZ" FontSize="16" Padding="10"
                                 Background="{DynamicResource FondoInput}" Foreground="{DynamicResource TextoPrincipal}"
                                 BorderBrush="{DynamicResource ColorAccento}" BorderThickness="1" Margin="0,0,0,20"
                                 PasswordChanged="TxtContrasenaZ_Changed"/>

                    <Button Content="REALIZAR CIERRE Z" Command="{Binding RealizarCierreCommand}"
                            FontSize="16" FontWeight="Bold" Padding="15,12"
                            Background="{DynamicResource ColorAccento}" Foreground="White" BorderThickness="0"
                            Cursor="Hand" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="6" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

                <!-- Despues del cierre -->
                <StackPanel Visibility="{Binding CierreRealizado, Converter={StaticResource BoolToVisibility}}">
                    <Border Background="{DynamicResource FondoInput}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="RESUMEN DEL CIERRE" FontSize="14" FontWeight="Bold"
                                       Foreground="{DynamicResource ColorExito}" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Total perchas:" Foreground="{DynamicResource TextoSecundario}" FontSize="14"/>
                                <TextBlock Grid.Column="1" Text="{Binding TotalTickets}"
                                           Foreground="{DynamicResource TextoPrincipal}" FontSize="14" FontWeight="Bold"/>
                            </Grid>

                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Total prendas:" Foreground="{DynamicResource TextoSecundario}" FontSize="14"/>
                                <TextBlock Grid.Column="1" Text="{Binding TotalPrendas}"
                                           Foreground="{DynamicResource TextoPrincipal}" FontSize="14" FontWeight="Bold"/>
                            </Grid>

                            <Border BorderBrush="{DynamicResource BordeCampo}" BorderThickness="0,1,0,0" Margin="0,5,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="TOTAL RECAUDADO:" Foreground="{DynamicResource TextoPrincipal}"
                                           FontSize="16" FontWeight="Bold"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding TotalRecaudado, StringFormat='{}{0:C2}'}"
                                           Foreground="{DynamicResource ColorAccento}" FontSize="20" FontWeight="Bold"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Button Content="IMPRIMIR CIERRE" Command="{Binding ImprimirCierreCommand}"
                            FontSize="14" FontWeight="Bold" Padding="15,10"
                            Background="{DynamicResource ColorExito}" Foreground="White" BorderThickness="0"
                            Cursor="Hand" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="6" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

                <TextBlock Text="{Binding Mensaje}" FontSize="12"
                           HorizontalAlignment="Center" Margin="0,10,0,0"
                           TextWrapping="Wrap"
                           Visibility="{Binding Mensaje, Converter={StaticResource StringToVisibility}}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource ColorExito}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HayError}" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource ColorError}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Button Content="Volver" Command="{Binding CerrarCommand}"
                        Background="Transparent" Foreground="{DynamicResource TextoSecundario}" BorderThickness="0"
                        Cursor="Hand" FontSize="12" Margin="0,15,0,0"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
